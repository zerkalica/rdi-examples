config:
    router:
        isFull: false
        routes:
            index:
                pattern: (/)
                page: TodoPage
            TodoPage:
                pattern: /(<group>)
                page: TodoPage
                defaults:
                    group: all
