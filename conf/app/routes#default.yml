config:
    router:
        isFull: false
        routes:
            index:
                pattern: (/)
                page: TodoPage
                defaults:
                    group: all
            TodoPage:
                pattern: /(<group>)
                page: TodoPage
                defaults:
                    group: all
